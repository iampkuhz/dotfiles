# ========= 只保留必须的密钥/密码 =========

# 用于 Web 会话签名（可用 `openssl rand -base64 32` 生成）
LANGFUSE_NEXTAUTH_SECRET=replace-with-random-string

# 用于 Langfuse 内部加密（可用 `openssl rand -base64 32` 生成）
LANGFUSE_SALT=replace-with-random-string

# 必须是 64 位十六进制（`openssl rand -hex 32`）
LANGFUSE_ENCRYPTION_KEY=replace-with-64-hex

# 数据库/缓存/对象存储密码
POSTGRES_PASSWORD=replace-with-postgres-password
CLICKHOUSE_PASSWORD=replace-with-clickhouse-password
REDIS_PASSWORD=replace-with-redis-password
MINIO_PASSWORD=replace-with-minio-password
