# 请把以下变量写入 ~/.env（由 zsh 启动时自动加载）。

# LiteLLM 网关访问 token（OpenClaw 调用时用这个鉴权）
LITELLM_MASTER_KEY=replace-with-a-long-random-token

# Langfuse 项目密钥（在 Langfuse Project Settings 中复制）
LANGFUSE_PUBLIC_KEY=pk-lf-replace-me
LANGFUSE_SECRET_KEY=sk-lf-replace-me

# ===== lmstudio 模式必填 =====
# 示例：openai/qwen/qwen3-14b
LMSTUDIO_MODEL_ID=openai/replace-with-lmstudio-model-id

# ===== remote 模式必填 =====
# 示例：openai/qwen3.5-plus
REMOTE_MODEL_ID=openai/replace-with-remote-model-id
# 示例：https://dashscope.aliyuncs.com/compatible-mode/v1
REMOTE_UPSTREAM_BASE_URL=https://replace-with-your-remote-endpoint/v1
REMOTE_UPSTREAM_API_KEY=replace-with-remote-api-key
